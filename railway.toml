[build]
  nixpacks = "# Python / FastAPI — Railway auto-detects from requirements.txt"

[deploy]
  numReplicas = 1
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
  startCommand = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT}"
